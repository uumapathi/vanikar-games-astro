# SEO Round 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the updated SEO recommendations (vanikar-games-seo-recommendations-updated.md) — consistency, depth, and internal-linking improvements to existing pages.

**Architecture:** All changes ride on the existing localized component system: components carry per-locale copy objects, game data drives game pages, feature pages are EN-only. New capabilities: per-game SEO overrides, related-game links, extra FAQs, noindex for untranslated locale pages.

**Tech Stack:** Astro 5, TypeScript data files, @astrojs/sitemap.

**Scope (mapped to doc priorities):**
- P1 §1: Fix launch-status contradiction — hero CTA + DownloadSection "Available Now" → launching-soon messaging (6 locales). FeatureLanding CTAs relabeled.
- P2 §2 + P9 §14: Homepage H1 → "Play Classic Card Games Online & Offline" (tagline becomes H1; brand name stays visually but is no longer the H1). Subtitle → §14 supporting copy. 6 locales.
- P3 §5: `noindex` for localized game pages whose overlay lacks a full translation (no `description`); BaseLayout gains `noindex` prop; sitemap filter excludes them.
- P4 §4: Indian Jackass — doc-recommended title/H1 via new per-game `seo` override; playGuide gains "An example round" + "Common variations"; extra FAQs.
- P5 §7: `relatedGames` slugs in data for all available games; game page "More Games" → contextual "Related Games" (localized heading).
- P6 §9–11: Deepen offline / with-friends / against-computer feature pages with long-tail sections + FAQs.
- P8 §12: "casino-quality" → premium phrasing in AppShowcaseSection (6 locales). ("Real stakes" already gone.)
- §21: Homepage FAQ section (new localized HomeFaqSection + FAQPage JSON-LD) on all 6 homepages; hearts gets game-specific extra FAQs.
- OUT: full translations (§5 option 1), category pages (§19), GSC-driven content (§25) — future.

---

### Task 1: Data model + Indian Jackass + related games (`src/data/games.ts`)
- [ ] Add `Faq`, `seo?: {title?, h1?}`, `relatedGames?: string[]`, `extraFaqs?: Faq[]` to Game interface
- [ ] indian-jackass: seo override (title "Indian Jackass Card Game – Rules, How to Play & Strategy | Vanikar", h1 "How to Play Indian Jackass"), playGuide sections "An example round" + "Common variations", extraFaqs (what is it, rules consistency, penalty cards)
- [ ] hearts: extraFaqs (Queen of Spades value, shooting the moon, two players)
- [ ] relatedGames for all 11 available games per doc §7 + §18 clusters

### Task 2: noindex plumbing
- [ ] `src/data/games.i18n.ts`: export `isGameFullyLocalized(slug, locale)` (EN → true; else overlay has `description`)
- [ ] `src/layouts/BaseLayout.astro`: `noindex?: boolean` prop → `<meta name="robots" content="noindex" />`
- [ ] `astro.config.mjs`: sitemap filter excludes untranslated localized game URLs (import helper; fall back to no filter change if config cannot import TS)

### Task 3: Game page template (`src/components/GameDetailPage.astro`)
- [ ] Apply `game.seo` title/h1 overrides (EN only)
- [ ] Merge `game.extraFaqs` into faqs (page + JSON-LD) for EN
- [ ] Related Games: use `game.relatedGames` (fallback: current first-4); heading localized "Related Games"
- [ ] Pass `noindex={!isGameFullyLocalized(slug, lang)}` to BaseLayout

### Task 4: Launch-status + wording (6 locales each)
- [ ] HeroSection: brand title becomes non-H1; H1 = "Play Classic Card Games Online & Offline" (localized); subtitle = §14 support copy; primary CTA "🚀 Coming Soon — Explore the Games" → /games; secondary "🔔 Get Notified at Launch" → #download
- [ ] DownloadSection: tag "♠ Available Now" → "♠ Launching Soon"
- [ ] AppShowcaseSection: "casino-quality table" → "premium card table" phrasing
- [ ] FeatureLanding: "⬇ Download Free" CTAs → "🔔 Get Notified at Launch"

### Task 5: Feature-page depth (EN pages)
- [ ] offline-card-games: sections (flights/travel, two players, beginners) + FAQs (requires internet?, AI offline?, switch online/offline?)
- [ ] card-games-with-friends: sections (best picks by group size, how private tables work) + FAQs
- [ ] card-games-against-computer: sections (why AI, beginner picks, strategy picks, difficulty) + FAQs

### Task 6: Homepage FAQ
- [ ] New `src/components/HomeFaqSection.astro` (7 Q&As × 6 locales + FAQPage JSON-LD via head-free inline script)
- [ ] Add before DownloadSection on all 6 homepage files

### Task 7: Verify + ship
- [ ] `npm run build` clean; spot-check indian-jackass, a noindexed es game page, homepage H1, sitemap exclusions
- [ ] Commit per task group, push
